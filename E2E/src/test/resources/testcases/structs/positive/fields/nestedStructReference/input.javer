struct Inner {
    int value;
}

struct Outer {
    struct Inner inner;
}

fn void main() {
    let struct Inner i = new struct Inner;
    let struct Outer o = new struct Outer;
    i.value = 9;
    o.inner = i;
    call printi(o.inner.value);
}
