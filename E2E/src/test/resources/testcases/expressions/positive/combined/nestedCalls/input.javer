fn int doubleIt(int x) {
    return x * 2;
}

fn int addOne(int x) {
    return x + 1;
}

fn void main() {
    let int r = call addOne(call doubleIt(call addOne(3)));
    call printi(r);
    return;
}
