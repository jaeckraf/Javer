fn int sumArray(int[] arr, int length) {
    let int total = 0;
    for (let int i = 0; i < length; i++) {
        total += arr[i];
    }
    return total;
}

fn void main() {
    let int[] data = {1, 2, 3, 4, 5};
    let int s = call sumArray(data, 5);
    call printi(s);
    return;
}
