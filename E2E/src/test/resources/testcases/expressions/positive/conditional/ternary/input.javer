fn void main() {
    let int a = 5;
    let int b = 10;
    let int max = a > b ? a : b;
    let int min = a < b ? a : b;
    call printi(max);
    call printi(min);

    let boolean flag = true;
    let int x = flag ? 1 : 2;
    call printi(x);
    return;
}
