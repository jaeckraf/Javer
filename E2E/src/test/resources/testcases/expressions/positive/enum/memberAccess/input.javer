enum Color {
    RED,
    GREEN = 5,
    BLUE
}

fn void main() {
    let enum Color c = Color.RED;
    let enum Color g = Color.GREEN;
    let enum Color b = Color.BLUE;
    call printi(c);
    call printi(g);
    call printi(b);
    return;
}
