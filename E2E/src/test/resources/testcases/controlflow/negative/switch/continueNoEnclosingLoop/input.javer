fn void main() {
    call prints("Testing continue outside loop (should fail):\n");
    switch (1) {
        case 1: {
            call prints("Matched 1\n");
            continue; // Invalid: switch is not a loop
        }
        default:
            call prints("Default\n");
    }
    call prints("Should not reach here.\n");
}