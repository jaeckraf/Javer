fn void main() {
    call prints("Testing break outside loop (should fail):\n");
    switch (1) {
        case 1: {
            call prints("Matched 1\n");
            break; // Invalid: switch is not a loop
        }
        default:
            call prints("Default\n");
    }
    call prints("Should not reach here.\n");
}