# JAR Configuration - automatically filled by Maven during build
# These paths are relative to the working directory during development
# For packaged applications, they will be adjusted per profile

# Compiler JAR path
javer.compiler.jar=${javer.compiler.jar}

# VM JAR path
javer.vm.jar=${javer.vm.jar}

