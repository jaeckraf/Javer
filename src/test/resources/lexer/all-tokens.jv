fn void main(int a, double b, boolean c, string s, char d, struct e, enum f) {
    let x = 42; let y = 3.14; let h = 0xFF; let o = 0o77; let bn = 0b1010;
    let str = "hi\n"; let ch = 'a'; let t = true; let n = null;
    x = a + b - c * d / e % f;
    x = a > b;
    if (x == 0) return; else x++;
    while (x < 10) x += 1;
    do x -= 1;
    for (;;) x *= 2;
    switch (x) { case 1: default: break; continue; }
    call main;
    x--; x != y; x <= y; x >= y; !x; x && y; x || y;
    x & y; x | y; x ^ y; ~x; x << 1; x >> 1;
    x <<= 1; x >>= 1; x &= 1; x |= 1; x ^= 1;
    x /= 2; x %= 2;
    arr[0].field;
    x ? y : d;
    false;
}
