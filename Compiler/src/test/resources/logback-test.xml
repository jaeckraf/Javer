<configuration>
    <property name="LOG_DIR" value="logs"/>
    <property name="MODULE" value="${MODULE:-compiler-test}"/>
    <property name="LOG_LEVEL" value="${LOG_LEVEL:-INFO}"/>
    <property name="PATTERN"
              value="%d{HH:mm:ss} %-7level %-24.24logger{0} %msg%n"/>

    <appender name="MODULE_FILE" class="ch.qos.logback.core.FileAppender">
        <file>${LOG_DIR}/javer-${MODULE}.log</file>
        <append>false</append>
        <encoder>
            <pattern>${PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="${LOG_LEVEL}">
        <appender-ref ref="MODULE_FILE"/>
    </root>
</configuration>

