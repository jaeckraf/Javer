fn int pick(int value) {
    switch (value) {
        case 0: {
            return 0;
        }
        default: {
            let int ignored = value;
        }
    }
}

fn void main() {
    let int result = call pick(1);
}
